---
sidebar_position: 6
---

# CalendarGrid Component

**Inner Architecture**.

```jsx live noInline
/**
 * to avoid having the page crashing on parsing code we use the noInline
 * therefore the function `render` is required
 */

const CalendarGridComponent = () => {
  return (
    <FlexCol>
      <CalendarFullDayEventBar eventHeight={2} />
      <Box>
        <CalendarGrid />
      </Box>
    </FlexCol>
  );
};

render(<CalendarGridComponent />);
```
