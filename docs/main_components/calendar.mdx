---
sidebar_position: 1
---

# Calendar Component

**Inner Architecture**.

```jsx live noInline
/**
 * to avoid having the page crashing on parsing code we use the noInline
 * therefore the function `render` is required
 */

const CalendarComponent = () => {
  return (
    <FlexRow width="100%" height="100%" p={4}>
      <FlexCol width="664px">
        <CalendarHeader />
        <CalendarGrid />
      </FlexCol>
    </FlexRow>
  );
};

render(<CalendarComponent />);
```
