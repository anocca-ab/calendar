---
sidebar_position: 1
---

# Tutorial Intro

Let's discover **Calendar in less than 5 minutes**.

```jsx live noInline
/**
 * to avoid having the page crashing on parsing code we use the noInline
 * therefore the function `render` is required
 */

const CalendarEntryPoint = () => {
  return (
    <FlexRow width="100%" p={4}>
      <Calendar />
    </FlexRow>
  );
};

render(<CalendarEntryPoint />);
```

import { SandPackPlayground } from "@site/src/components/SandPackPlayground";

<SandPackPlayground />
